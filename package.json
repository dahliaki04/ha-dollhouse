{
  "name": "ha-dollhouse",
  "version": "0.1.0",
  "description": "Home Assistant card: draw rooms in seconds, drag entities onto a 2D/3D floor plan",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "lit": "^3.2.0",
    "three": "^0.170.0",
    "@types/three": "^0.170.0"
  }
}

